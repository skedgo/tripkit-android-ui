<?xml version="1.0" encoding="utf-8"?>
<com.skedgo.tripkit.ui.map.SimpleCalloutView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/leftImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/spacing_small"
        android:scaleType="fitCenter"
        android:visibility="gone"
        tools:src="@drawable/ic_check"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="240dp"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/titleView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="marquee"
            android:maxLines="2"
            android:textColor="@color/v4_title_text"
            android:textSize="16sp"
            tools:text="2:07pm" />

        <TextView
            android:id="@+id/snippetView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="marquee"
            android:singleLine="true"
            android:textSize="14sp"
            tools:text="Awesome place" />
    </LinearLayout>

    <ImageView
        android:id="@+id/rightImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/spacing_small"
        android:scaleType="fitCenter"
        android:visibility="gone"
        tools:src="@drawable/ic_arrow_forward"
        tools:visibility="visible" />

    <ImageView
        android:id="@+id/infoImageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/spacing_small"
        android:scaleType="fitCenter"
        android:visibility="gone"
        tools:src="@drawable/ic_icon_info"
        tools:visibility="visible" />
</com.skedgo.tripkit.ui.map.SimpleCalloutView>